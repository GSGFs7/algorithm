#include <bits/stdc++.h>
using namespace std;

const int N = 3e5 + 10;
int g[N][N];
int n, m;

int main()
{
#ifdef __LOCAL__
    freopen("1.in", "r", stdin);
    freopen("1.out", "w", stdout);
#endif

    cin >> n >> m;
    for (int i = 0; i < m; i++)
    {
        
    }
}